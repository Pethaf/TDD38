#include <iterator>

class Asscending 
{
public:
	template <typename T> 
	static bool compare(const T& lhs, const T& rhs)
	{

		return lhs < rhs; 
	)
};

template <typename T, class Order_Policy = Asscending> 
class Sort 
{

	
}; 